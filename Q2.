# Find all the even numbers and store them in a new list:

numbers = list(map(int, input("Enter numbers separated by spaces: ").split()))
even_numbers = [num for num in numbers if num % 2 == 0]
print(f"Even numbers: {even_numbers}")
